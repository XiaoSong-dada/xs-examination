# 2026_03_24_规范文档迁移与依赖拓扑整理计划

## 1. 目标

本计划用于完成当前规范文档整理的第一阶段工作，重点解决三件事：

1. 将旧 `.copilot` 体系中仍有价值的规范与资料迁移到标准位置。
2. 把“业务定位先看依赖拓扑、再看最短 e2e 链路”的规则明确写入新的规范体系。
3. 基于实际代码使用情况，确认 `packages/shared-types` 当前是否仍有保留必要，并给出后续处理决策。

## 2. 范围

本计划纳入范围：

1. 旧 `AGENTS` 迁移清单整理。
2. 旧依赖拓扑图从 `.copilot` 迁移到 `doc/`。
3. 新规范中补充“先看拓扑图，再看 e2e”的定位约定。
4. 核对 `packages/shared-types` 的真实使用情况，并形成处理结论。

本计划不纳入范围：

1. 立即删除 `.copilot/AGENTS.md`、`.copilot/memorybank.md`、`.copilot/project-dependency-map.md`。
2. 立即删除 `packages/shared-types` 及其在 package.json 中的依赖声明。
3. 立即更新已过时的 PRD、TECH_DESIGN、RESEARCH 正文。

## 3. 当前现状

### 3.1 新规范入口已建立

- 全局工作区说明已落到 [.github/copilot-instructions.md](../../.github/copilot-instructions.md)。
- 细粒度规范已拆到 [.github/instructions](../../.github/instructions)。

### 3.2 旧 `.copilot` 资料仍是历史来源

- 旧 `AGENTS`、`project-dependency-map` 仍保留在 `.copilot/`，作为迁移期历史资料来源。
- `project-dependency-map.md` 已经完成正式迁移，现由 `doc/project_dependency_topology.md` 承接文档职责。
- 旧 `memorybank.md` 已确认与当前仓库事实不符，且无独立有效信息，已从 `.copilot/` 删除。

### 3.3 shared-types 当前是声明依赖，不是实际源码依赖

已确认事实：

1. [apps/student/package.json](../../apps/student/package.json) 仍声明 `@xs/shared-types`。
2. [apps/teacher/package.json](../../apps/teacher/package.json) 仍声明 `@xs/shared-types`。
3. [packages/shared-types/src/index.ts](../../packages/shared-types/src/index.ts) 当前导出 `exam.ts` 与 `protocol.ts`。
4. 在 `apps/**` 范围内检索 `@xs/shared-types`，仅命中两个 package.json 依赖声明，没有任何前后端源码 import。
5. 旧依赖图谱也已经将 `shared-types` 描述为“入口已预留、实际收敛未完成”的状态。

据此，当前更合理的判断是：`packages/shared-types` 不是现网代码路径中的必要依赖，而是未被消费的预留结构，应从当前规范口径中移出，并作为后续仓库清理项处理。

## 4. 迁移清单

### 4.1 `AGENTS` 条目处理清单

#### 已迁移到 `.github`

1. 构建与校验命令。
2. monorepo 架构边界。
3. 前端通过 `services` 封装 Tauri 调用。
4. `@/` 路径别名约定。
5. Rust 命名、DTO/schema 放置、避免 `unwrap/expect`、schema 只走 migration。
6. 业务闭环先读 e2e，并在链路变化后同步更新 e2e。

补充说明：

- “跨端协议放 `packages/shared-types`” 这条旧口径虽然曾被迁入 `.github`，但已在本计划阶段三中确认需要撤回，不再视为当前有效规范。

#### 待确认后再迁移

1. Git 提交规范。
2. 版本里程碑边界。
3. 旧 `.copilot` 文档是否仍保留为启动检查项。

#### 建议废弃，不再进入自动规范

1. 已过时的 React / Ant Design / Router 版本锁定。
2. “所有函数必须写完整注释”的强约束。
3. 尚未验证为当前实现基线的数据安全整段要求。
4. 当前代码中未落地的 anti-cheat 目录与系统级钩子约束。

### 4.2 旧依赖拓扑图处理清单

1. 将旧 `.copilot/project-dependency-map.md` 的有效内容迁移为正式文档 [doc/project_dependency_topology.md](../project_dependency_topology.md)。
2. 新文档保留模块入口、快速定位和业务到 e2e 映射。
3. 新文档移除“必须读取旧 memorybank / 旧 AGENTS”的依赖口径。
4. 旧 `.copilot/project-dependency-map.md` 先保留，待审阅通过后删除。

### 4.3 shared-types 处理清单

1. 已在本计划中确认：`shared-types` 不是已被源码真实消费的共享层，而是未使用的预留层。
2. 本阶段决策为：清除 `packages/shared-types`，并将其视为后续仓库清理项。
3. 清理时同步移除 teacher 与 student 的 package.json 依赖声明，以及 `.github` 中仍以 `shared-types` 作为现行共享协议入口的描述。
4. 若后续版本重新引入跨端共享类型，应先基于真实调用链确认落点，再重新建立该包，而不是继续保留空转目录。

## 5. 已确认决策

1. 正式规范入口以 `.github` 为准，不再以 `.copilot` 作为自动加载入口。
2. 正式项目资料入口以 `doc/` 为准，依赖拓扑图应迁移到 `doc/`。
3. 业务逻辑定位时，优先读依赖拓扑图；若拓扑图已映射最短 e2e，再继续读对应 e2e 文档。
4. `packages/shared-types` 已确认应被清除；本轮先完成计划结论与规范迁移落点，实际包删除与依赖清理由后续仓库改动执行。
5. shared-types 相关规范撤回与前后端规范补齐，转入专门计划 [doc/plans/2026_03_24_前后端规范差异补齐与迁移计划.md](2026_03_24_前后端规范差异补齐与迁移计划.md)。

## 6. 依赖定位

本次计划直接依赖以下文档或文件：

1. 旧规范来源：[.copilot/AGENTS.md](../../.copilot/AGENTS.md)
2. 旧依赖拓扑来源：[.copilot/project-dependency-map.md](../../.copilot/project-dependency-map.md)
3. 新全局规范：[.github/copilot-instructions.md](../../.github/copilot-instructions.md)
4. 新拆分规范目录：[.github/instructions](../../.github/instructions)
5. 现有最短链路目录：[doc/e2e](../e2e)
6. shared-types 包：[packages/shared-types](../../packages/shared-types)

## 7. 分阶段实施计划

### 阶段一：规范与资料迁移落点整理

1. 已新增 `doc/project_dependency_topology.md`，接管旧依赖图谱的正式文档职责。
2. 已在 `.github/copilot-instructions.md` 中声明“业务逻辑先看依赖拓扑图，再看对应 e2e”。
3. 已在本计划文档中固化迁移清单与 shared-types 当前结论。

### 阶段二：旧 `.copilot` 文档清理准备

1. 已审阅新拓扑图与迁移清单，确认 `project-dependency-map.md` 的正式职责已由 `doc/project_dependency_topology.md` 承接。
2. 已核对 `.copilot/memorybank.md`，确认其包含“代码未启动”“旧工作目录 E:\code\xs-examination”“旧技术版本”等失真信息，且无未迁移的唯一有效信息。
3. 已删除 `.copilot/memorybank.md`；旧 `.copilot/project-dependency-map.md` 与 `.copilot/AGENTS.md` 继续保留为过渡期历史参考。

### 阶段三：shared-types 清理决策

1. 已再次确认：在 `apps/**` 范围内检索 `@xs/shared-types`，仅命中两个 package.json 依赖声明，没有实际源码 import。
2. 已形成阶段决策：清除 `packages/shared-types`，后续执行时同步删除两端依赖声明。
3. shared-types 撤口径、前后端注释规范补齐、目录职责约定补齐，转入专门计划 [doc/plans/2026_03_24_前后端规范差异补齐与迁移计划.md](2026_03_24_前后端规范差异补齐与迁移计划.md)。

## 8. 风险与边界

1. 旧依赖图谱包含大量结构化摘要，迁移时若压缩过度，可能损失一线排查价值。
2. `packages/shared-types` 虽然当前未被源码 import，但仍被 package.json 声明依赖，且 `.github` 体系中仍有多处沿用旧口径；清理时需要同步处理依赖声明与规范文本，避免留下悬空说明。
3. 旧 `.copilot/AGENTS.md` 与 `.copilot/project-dependency-map.md` 仍保留，若后续删除前未补充废弃说明，可能继续误导阅读顺序。
4. PRD、TECH_DESIGN、RESEARCH 已知过时，本计划不会替代这些文档的整体更新，只先处理规范入口和依赖拓扑。

## 9. 最小验收步骤

1. `doc/` 下存在新的依赖拓扑图正式文档。
2. 全局规范中已声明“业务逻辑先看拓扑图，再看 e2e”。
3. 本计划文档中已包含完整迁移清单。
4. 本计划文档中已明确 `packages/shared-types` 当前无实际源码 import，且阶段三已形成清除决策。
5. 已确认失真的 `.copilot/memorybank.md` 被删除，且 README 与旧 AGENTS 中不再保留对它的有效入口引用。
6. 已新增用于承接后续规范补齐工作的专门计划文档。

## 10. 关联文档

1. [doc/project_dependency_topology.md](../project_dependency_topology.md)
2. [.github/copilot-instructions.md](../../.github/copilot-instructions.md)
3. [doc/e2e](../e2e)
4. [.copilot/AGENTS.md](../../.copilot/AGENTS.md)
5. [.copilot/project-dependency-map.md](../../.copilot/project-dependency-map.md)
6. [doc/plans/2026_03_24_前后端规范差异补齐与迁移计划.md](2026_03_24_前后端规范差异补齐与迁移计划.md)

