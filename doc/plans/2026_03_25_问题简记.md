# 待完善功能
TODO: 当学生端与教师端考试状态不同步时解决方案（学生端通过特殊手段改变了本地考试状态）
- 当学生端向教师端发送同步请求后，如果教师端的考试状态变为了已结束，那么学生端本地的考试状态也应该变为已结束
- 反之亦然，如果教师端的考试状态变为了未开始，那么学生端本地的考试状态也应该变为未开始
- 解决方案：在学生端发送同步请求后，教师端应该返回当前的考试状态，学生端根据教师端返回的状态来更新本地的考试状态

# 待修复问题
- 教师端的问题导入外键有冲突
- - 这里的猜测是再批量导入前忘记清空当前考试的题库

# 后续更新计划
[ ] - 更强的状态约束
- - 现在对于状态的约束不强,当遇到已归档的考试时或已结束的考试是不可以再重新开靠
[ ] - 学生端初始化
- - 现在的学生端的初始化只有初始化姓名以及教师端ip
- - 后续添加初始化问题库 - 即将问题表清空
[ ] - 选项类型
- - 更新选项类型，现在的类型只有text一种，后续引入图片类型
[ ] - 题目优化
- - 当前版本的题目为text类型，后续引入图片题目附件（已预留更新插槽）
[ ] - 优化学生端页面
- - 当题目中不存在图片时，需将图片的组件隐藏
